for (i = 0; i < spocRating.length; i++) {
  var value = $("#" + spocRating[i].id).val();
  if (value === "" || !/^\d+$/.test(value)) { // This regex checks if value is not an integer
    isValid = false;
    errorMsg = "Please enter an integer rating for all skills.";
    break;
  }
}
