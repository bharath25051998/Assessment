SELECT column1, column2, 
       CASE 
           WHEN column1 = 0 THEN NULL -- Handle division by zero
           ELSE (column2 * 100.0 / column1) 
       END AS percentage
FROM (
    SELECT COALESCE(column1, 0) AS column1, COALESCE(column2, 0) AS column2
    FROM your_table
) AS subquery_alias;
