SELECT
        CONCAT(DATENAME(MONTH, ad.ERD), '-', DATEPART(YEAR, ad.ERD)) AS Month_Year,
        COUNT(*) AS ERD_Match_Count
    FROM PSEmployeeAdditionalDetails ad
    WHERE ad.ERD IS NOT NULL
    GROUP BY CONCAT(DATENAME(MONTH, ad.ERD), '-', DATEPART(YEAR, ad.ERD))

