@{
    ViewBag.Lobs = new List<string> { "LOB1", "LOB2", "LOB3", /* Add all LOBs here */ };
}

<div class="dashboard-container">
    <div class="search-filter">
        <!-- Search filter UI -->
        <input type="text" id="searchInput" placeholder="Search...">
        <!-- Add any other filter options here -->
    </div>

    <div class="dashboard-columns">
        @foreach (var lob in ViewBag.Lobs)
        {
            <div class="column">
                <h2>@lob</h2>
                <div id="@($"cards-{lob}")" class="card-container">
                    <!-- Cards for each LOB will be dynamically added here -->
                </div>
            </div>
        }
    </div>
</div>

// Function to populate cards for each LOB
function populateCards(lob) {
    // Make an AJAX call to fetch data for the specified LOB
    // and populate the cards dynamically
}

// Event listener for expanding cards
document.addEventListener('click', function(event) {
    if (event.target.classList.contains('expand-button')) {
        // Redirect to another view for more details
        // You can use the data attributes of the card to pass relevant information
    }
});

// Event listener for search filter
document.getElementById('searchInput').addEventListener('input', function(event) {
    var searchQuery = event.target.value.toLowerCase();
    // Filter cards based on the search query
});
