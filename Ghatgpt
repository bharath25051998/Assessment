traces
| where timestamp >= ago(1d)
| where message has "Running task"
| extend ServiceName = extract(@"Running task (\w+)", 1, message, typeof(string))
| project timestamp, message, ServiceName
| order by timestamp desc
