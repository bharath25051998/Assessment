$(document).ready(function () {
    // Add buttons to existing DataTable instance
    var dataTable = $('table.display').DataTable();
    new $.fn.dataTable.Buttons(dataTable, {
        buttons: [
            'copy', 'csv'
        ]
    });
    dataTable.buttons().container().appendTo($('.dataTables_length'));
});
