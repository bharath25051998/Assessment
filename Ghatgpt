requests
| where timestamp >= ago(1d)
| where success == false
| project requestTime=timestamp, requestName=name, url, resultCode, operation_Id
| join kind=leftouter (
    exceptions
    | project exceptionTime=timestamp, operation_Id, type, outerMessage
) on operation_Id
| order by requestTime desc
