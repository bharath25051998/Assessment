You have two tables with a primary–foreign key dependency:
	•	Employee
	•	id (int, PK)
	•	name (varchar(max))
	•	emailid (varchar(max))
	•	grade (varchar(max))
	•	CourseCompletion
	•	empid (FK → Employee.id)
	•	coursename (varchar(max))
	•	coursestatus (varchar(max))
	•	handsonstatus (varchar(max))

You want dummy data that respects the relationship. 
