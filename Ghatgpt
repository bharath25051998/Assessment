traces
| where timestamp >= ago(1d)
| where message has "Task " and message has " completed"
| project timestamp, message, ServiceName = extract(@"Task (.+) completed", 1, message, typeof(string))
| order by timestamp desc
