DECLARE @CurrentDate DATE = GETDATE()
DECLARE @StartDate DATE = DATEADD(QUARTER, DATEDIFF(QUARTER, 0, @CurrentDate), 0)
DECLARE @EndDate DATE = DATEADD(MONTH, 6, @StartDate)

SELECT @StartDate AS Current_Quarter_StartDate, @EndDate AS Next_2_Quarters_EndDate
