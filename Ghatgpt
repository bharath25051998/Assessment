SELECT 
    cs.remid,
    STRING_AGG(CONCAT(c.chapter_name, ': ', s.status_name), '; ') AS chapter_status
FROM 
    chapters c
JOIN 
    chapter_statuses cs
ON 
    c.chapter_id = cs.chapter_id
JOIN 
    statuses s
ON 
    cs.status_id = s.status_id
GROUP BY 
    cs.remid;
