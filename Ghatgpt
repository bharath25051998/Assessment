SELECT 
    lobs,
    SUM(CASE WHEN ddstatus = 'Approved' THEN quote ELSE 0 END) AS Approved,
    SUM(CASE WHEN ddstatus = 'Rejected' THEN quote ELSE 0 END) AS Rejected,
    FORMAT(SUM(quote), 'C', 'en-US') AS Total
FROM 
    appprofiledetails
GROUP BY 
    lobs WITH ROLLUP
