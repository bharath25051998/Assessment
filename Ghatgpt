// Print out or log the SQL query with parameters before executing the command
Console.WriteLine("SQL Query: " + command.CommandText);

// Print out or log the parameter values
Console.WriteLine("EmployeeEmail: " + command.Parameters["@EmployeeEmail"].Value);
Console.WriteLine("LOB: " + command.Parameters["@LOB"].Value);
Console.WriteLine("ChapterID: " + command.Parameters["@ChapterID"].Value);

try
{
    connection.Open();
    adapter.Fill(dataTable);
    
    // Process the DataTable as needed
    foreach (DataRow row in dataTable.Rows)
    {
        // Access data from the row
        Console.WriteLine(row["ColumnName"]);
    }
}
catch (Exception ex)
{
    Console.WriteLine("Error: " + ex.Message);
}
